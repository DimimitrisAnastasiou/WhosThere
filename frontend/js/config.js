// ── App Configuration ─────────────────────────────────────────────────────
// Update these values for your environment.
// In production, these are safe to expose (they are public keys).

export const CONFIG = {
  API_URL: "http://localhost:8000",          // change to Railway URL in prod
  SUPABASE_URL: "https://YOUR_PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR_ANON_KEY",
};
